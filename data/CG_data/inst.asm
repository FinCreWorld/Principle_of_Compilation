       MOV        ax,         1
       MOV        t0,        ax
       MOV        bx,        ax
       MOV         a,        bx
       MOV        cx,         2
       MOV        t1,        cx
       MOV        dx,        cx
       MOV         b,        dx
       MOV        bx,         a
       MOV        bx,         3
       MOV        t2,        bx
       MOV        dx,         b
       MOV        dx,        t2
       MOV         c,        dx
       MOV        dx,         c
       MOV        dx,         4
       MOV        t3,        dx
       MOV        ax,        t0
       MOV        ax,        t3
       MOV         d,        ax
       MOV         d,        ax
       MOV        ax,         a
       ADD        ax,         b
       MOV        t4,        ax
       MOV        ax,        t4
       MOV        ax,        t4
       MOV         e,        ax
       MOV        ax,         e
       MOV        ax,         e
       MUL        ax,         c
       MOV        t5,        ax
       MOV        ax,        t5
       MOV        ax,        t5
       MOV         f,        ax
       MOV        ax,         f
       MOV        ax,         e
       DIV        ax,         f
       MOV        t6,        ax
       MOV        ax,        t6
       MOV        ax,        t6
       MOV         g,        ax
       MOV        ax,         g
       MOV        ax,         a
       SUB        ax,         b
       MOV        t7,        ax
       MOV        ax,        t7
       MOV        ax,        t7
       SUB        ax,         c
       MOV        t8,        ax
       MOV        ax,        t8
       MOV        ax,        t8
       SUB        ax,         g
       MOV        t9,        ax
       MOV        ax,        t9
       MOV        ax,        t9
       MOV         h,        ax
       MOV         h,        ax
